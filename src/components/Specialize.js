import React from "react";
import { useTranslation } from "react-i18next";

const Specialize = (props) => {
  const { t } = useTranslation();

  return <div className="specialize"></div>;
};

export default Specialize;
